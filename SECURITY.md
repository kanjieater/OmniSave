# Security Policy

This is a personal/hobby project. Security vulnerabilities are welcome via GitHub Issues
using the bug report template — check the "Is this a security concern?" checkbox and add
the `security` label.

There is no bounty program and no SLA. Fixes happen when time allows.

**Supported versions:** latest release only.
