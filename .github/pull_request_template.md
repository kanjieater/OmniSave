## Summary

<!-- What does this PR do and why? -->

## Related Issue

Closes #

## Checklist

- [ ] Tests pass (`docker compose run --rm test`)
- [ ] Linting passes (`ruff check server/src/`)
- [ ] TypeScript checks pass (`npx tsc --noEmit` in `server/ui/`)
- [ ] PR is under ~500 lines changed (if larger, explain why below)
- [ ] Context docs updated if server behavior or API changed
